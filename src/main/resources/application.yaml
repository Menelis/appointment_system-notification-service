management:
  endpoint:
    health:
      probes:
        enabled: true
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true

spring:
  application:
    name: @project.artifactId@
  config:
    import: optional:configserver:http://config-server:8888
